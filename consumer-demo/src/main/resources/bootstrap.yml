spring:
  cloud:
    nacos:
      discovery:
        server-addr: ${REGISTER_HOST:192.168.99.100}:${REGISTER_PORT:8848}
      config:
        server-addr: ${REGISTER_HOST:192.168.99.100}:${REGISTER_PORT:8848}
        file-extension: yml
    sentinel:
      transport:
        dashboard: ${SENTINEL_DASHBOARD_HOST:192.168.99.100}:${SENTINEL_DASHBOARD_PORT:8021}
    bus:
      trace:
        enabled: true